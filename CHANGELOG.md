# Changelog

## v1.2.0

Layout contract change: `studio/` is gone.

- `archive/` and `desk/` sit at `~/dev` (and at this repo root).
- `archive` is a real directory, not a symlink.
- `configs` → `desk/config`, `DevOps` → `desk/runbooks` (unchanged names).
- Placement rules live in the root `README.md`.

## v1.1.0

English structure READMEs. Tree still used `studio/` as a wrapper.

## v1.0.0

First snapshot of the layout maps (Persian). Docs-only; no secrets or archives.
